type ProjectProps = {
  name: string;
  desc?: string[];
  figDesc: string;
  language: string;
  source: string;
  demo?: string;
  topics?: string[];
  img: string;
  status: boolean;
  solo: boolean;
};
